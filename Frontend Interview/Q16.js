
// block , inline,inline block

// answer :

/*
 
 1. In css we have some block elements (ex : div, p ,section)
                        inline elements (ex : span , img , label, input)

 2. Block elemets takes the full width so if we add a new block elements it alsways start with new line
 3. Inline elements only takes the width of it's content so that it will not start with new line 
 4. To add some extra padding and width to inline elements we have use inline block prperty
*/